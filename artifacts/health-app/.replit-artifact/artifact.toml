kind = "web"
previewPath = "/"
title = "Health & Fitness Tracker"
version = "1.0.0"
id = "artifacts/health-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22314

[services.development]
run = "pnpm --filter @workspace/health-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/health-app", "run", "build" ]
publicDir = "artifacts/health-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22314"
BASE_PATH = "/"
